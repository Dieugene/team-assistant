# Архитектура VS UI (Visualization Service)

**Дата:** 2025-01-24
**Статус:** Архитектура для MVP
**Связанные документы:**
- `00_docs/architecture/overview.md` — общая архитектура системы
- `00_docs/architecture/visualization.md` — требования к визуализации

---

## 1. Введение

### 1.1 Назначение

VS UI — сервис визуализации для наблюдаемости системы Team Assistant. Позволяет разработчикам и стейкхолдерам наглядно видеть что происходит в системе: диалоги пользователей, обработку агентами, reasoning traces.

### 1.2 Цели

**Наблюдаемость:**
- Показать что происходит в системе в реальном времени
- Дать понимание работы агентов (reasoning traces)
- Обеспечить детализацию по клику

**Отладка:**
- Понять почему агент повел себя странно
- Видеть полный поток данных от пользователя к пользователю
- Анализировать историю событий

**Демонстрация:**
- Наглядно показать механику сервиса стейкхолдерам
- Auto-mode для live демонстрации

### 1.3 Стратегия MVP

**Критично для MVP:**
- Timeline View (вертикальный)
- Users Panel (аккордеон)
- Details Panel (детализация по клику)
- Auto-mode (polling + auto-scroll)
- SIM Controls (Start/Stop/Resume)

**На будущее:**
- Graph View (связи между сущностями)
- Demo Flow View (анимированная демонстрация)
- Replay режима (проиграть историю заново)
- Экспорт/Save timeline

---

## 2. Процессная модель

### 2.1 Сценарии использования

#### Сценарий 1: Разработчик отлаживает систему

**Цель:** Понять почему агент повел себя странно

**User Journey:**
1. Запускает VS UI → нажимает `Start` в SIM Controls
2. В Users Panel видит SIM пользователей и их сообщения
3. В Timeline Panel видит события появляющиеся в реальном времени
4. Видит что в момент T агент X отправил уведомление
5. Кликает на событие → в Details Panel видит детали (reasoning trace)
6. Понимает проблему → останавливает SIM (`Stop`) для детального анализа
7. Может кликнуть на ссылки в Details Panel для перехода к связанным событиям

**Вопросы которые он отвечает:**
- "Что произошло?" — Timeline
- "Почему агент так решил?" — Reasoning traces в Details Panel
- "Какой контекст был у пользователя?" — User Context в Details Panel

---

#### Сценарий 2: Демонстрация системы

**Цель:** Показать стейкхолдерам как система работает

**User Journey:**
1. Запускает VS UI → нажимает `Start` в SIM Controls
2. VS UI в auto-mode (polling, auto-scroll)
3. На широком экране видит:
   - Users Panel: как SIM пользователи общаются
   - Timeline Panel: события обрабатываются в реальном времени
4. Все наглядно — "вот User A написал сообщение, вот агент обработал, вот User B получил уведомление"
5. Может поставить на паузу (`Stop`) для объяснения деталей

**Критично:** Должно быть наглядно, "схватывается за 3-5 секунд"

---

#### Сценарий 3: History Review (вернулся через час)

**Цель:** Посмотреть что произошло в отсутствие

**User Journey:**
1. Запускает VS UI (SIM не запускает)
2. Timeline загружает последние X минут событий (10 по умолчанию)
3. Листает историю, кликает на события для детализации
4. Использует Swimlane Filters для фокусировки на конкретных агентах/пользователях
5. При необходимости запускает SIM для наблюдения в реальном времени

---

### 2.2 Потоки данных при отладке

```
1. SIM → DialogueAgent.invoke()
   ↓
2. Users Panel обновляется (показывает сообщение)
   ↓
3. DialogueFragmentPublished → EventBus
   ↓
4. Timeline Panel обновляется (новое событие)
   ↓
5. ProcessingAgent видит событие → начинает обработку
   ↓
6. AgentActivityStart → EventBus
   ↓
7. Timeline Panel обновляется
   ↓
8. ProcessingAgent завершает → AgentResult → EventBus
   ↓
9. Timeline Panel обновляется
   ↓
10. ContextAgent видит notification → анализирует
    ↓
11. NotificationSent → EventBus
    ↓
12. Users Panel обновляется (пользователь-получатель получает сообщение)
```

---

## 3. Структура UI

### 3.1 Layout

```
┌───────────────────────────────────────────────────────────────────┐
│ VS UI                                               [SIM: ▶ Start] │
├──────────────┬───────────────────────────────────┬────────────────┤
│              │                                   │                │
│   USERS      │       TIMELINE (vertical)         │    DETAILS     │
│              │      (newest events on top)       │  (fixed right) │
│              │                                   │                │
│ [Alice] ▼    │  ┌─ SWIMLANES ─────────────────┐ │  Event:        │
│  Sent:       │  │ ☑ All Events                │ │  agent_result  │
│  "..."       │  │ ☑ Users                     │ │                │
│  Recv:       │  │ ☑ Agents                    │ │  Source:       │
│  "..."       │  │ ☐ TaskManager only          │ │  TaskManager   │
│              │  └─────────────────────────────┘ │                │
│ [Bob] ▼      │                                   │  Payload:      │
│  Sent:       │  [10:15:35] notification_sent    │  {             │
│  "..."       │    🔵 to:bob                     │    "result":   │
│  Recv:       │       ↑                          │    ...         │
│  "..."       │       │                          │  }             │
│              │  [10:15:34] context_agent 🔶     │                │
│ [Carol] ▼    │       ├─ Input: notification_456 │  Triggered by: │
│  ...         │       ├─ Thought: "User needs"   │  agent_start   │
│              │       └─ Result: send            │                │
│              │                                   │  Triggered:    │
│              │  [10:15:31] task_manager 🔶      │  notification  │
│              │       ├─ Input: dialogue_frag    │                │
│              │       ├─ Thought: "Create task"  │  ━━━━━━━━━━━━━━│
│              │       └─ Result: notification     │  [→ Jump to    │
│              │                                   │   linked event]│
│              │  [10:15:30] dialogue_fragment ⚪  │                │
│              │    alice: "Need to finish task"  │                │
│              │                                   │                │
└──────────────┴───────────────────────────────────┴────────────────┘
```

---

### 3.2 Users Panel (левая панель)

**Назначение:** Показать активных пользователей (SIM или реальных) и их коммуникации

**Тип:** Аккордеон (пользователи сверху-вниз, можно свернуть/развернуть)

**Содержимое пользователя:**
- Имя/ID
- Sent: последние отправленные сообщения
- Received: последние полученные сообщения

**Обновление:**
- Polling через `GET /api/users?since=X`
- Auto-update когда SIM running

**Детализация:**
- Клик на пользователе → в Details Panel показывается User Context
- Диалоговый буфер, история, delivered messages

---

### 3.3 Timeline Panel (центральная панель)

**Назначение:** Показать события во времени (вертикальный timeline, новые сверху)

**Библиотека:** D3.js (свобода для кастомной вертикальной визуализации)

**Свойства:**
- Newest events on top
- Auto-scroll в auto-mode
- Scrollable в history mode

**Swimlane Filter (фильтр над timeline):**
```
┌─ SWIMLANES ─────────────────────────┐
│ ☑ All Events                       │
│ ☑ Users                            │
│ ☑ Agents                           │
│ ☐ TaskManager only                 │
│ ☐ Alice only                       │
└─────────────────────────────────────┘
```

**На будущее:** Дублирование timeline с разными фильтрами параллельно для визуального сопоставления.

---

### 3.4 Details Panel (правая панель)

**Назначение:** Детализация выбранного события

**Тип:** Фиксированная панель справа

**Содержимое:**
- Event metadata (id, timestamp, type, source)
- Payload (зависит от типа события)
- Reasoning trace (для agent events)
- Links (triggered_by, triggered) с возможностью перехода

**Обновление:**
- По клику на событие в Timeline
- При переходе по ссылке — обновляется на выбранное событие

---

### 3.5 SIM Controls (шапка)

**Назначение:** Управление симуляцией

**Расположение:** Шапка VS UI справа

**Кнопки:**
- `▶ Start` — запуск SIM, начало polling
- `⏸ Stop` — пауза SIM, polling продолжается
- `⏵ Resume` — продолжение SIM

**Состояние:**
- Отображается текущий режим (Running / Paused / Stopped)

---

## 4. Модель данных

### 4.1 Timeline Event

Базовая сущность для отображения на timeline.

```typescript
interface TimelineEvent {
  // Метаданные
  id: string                    // Уникальный ID события
  timestamp: ISO8601            // Время события

  // Тип и источник
  type: EventType               // Тип события
  source: EventSource           // Кто создал (user:alice, agent:TaskManager)
  source_type: 'user' | 'agent' | 'system'

  // Для swimlane фильтрации
  swimlane_id: string           // user_id или agent_name

  // Ссылки на связанные события
  links: {
    triggered_by: string[]      // ID событий которые вызвало это
    triggered: string[]         // ID событий которые вызвало это событие
  }

  // Полезная нагрузка (разная для разных типов)
  payload: EventPayload
}
```

---

### 4.2 EventType

Типы событий для отображения на timeline.

```typescript
enum EventType {
  // Пользовательские события
  MESSAGE_SENT = 'message_sent',
  MESSAGE_RECEIVED = 'message_received',

  // События EventBus
  DIALOGUE_FRAGMENT_PUBLISHED = 'dialogue_fragment_published',

  // Агентские события
  AGENT_ACTIVITY_START = 'agent_activity_start',
  AGENT_REASONING_STEP = 'agent_reasoning_step',
  AGENT_RESULT = 'agent_result',

  // Notification события
  NOTIFICATION_GENERATED = 'notification_generated',
  NOTIFICATION_SENT = 'notification_sent',

  // System события
  SYSTEM_START = 'system_start',
  SIM_START = 'sim_start',
  SIM_STOP = 'sim_stop',
}
```

---

### 4.3 EventPayload

Зависит от типа события.

**Для DIALOGUE_FRAGMENT_PUBLISHED:**
```typescript
{
  dialogue_fragment: {
    user_id: string
    messages: Array<{
      role: 'user' | 'assistant'
      content: string
      timestamp: ISO8601
    }>
  }
}
```

**Для AGENT_ACTIVITY_START / AGENT_RESULT:**
```typescript
{
  agent_name: string
  input: any                      // Входные данные из EventBus
  reasoning_trace?: ReasoningTrace  // Для AGENT_RESULT
  result?: any                    // Результат обработки
}
```

**Для ReasoningTrace (SGR):**
```typescript
{
  agent_id: string
  timestamp: ISO8601
  steps: Array<{
    phase: 'reasoning' | 'action' | 'observation'
    content: any                  // Зависит от SGR фреймворка
  }>
  final_action: string
  final_result: any
}
```

**Для NOTIFICATION_SENT:**
```typescript
{
  notification_id: string
  to_user: string
  message: string
  context: any                    // Опциональный контекст
}
```

---

### 4.4 Color Coding (Вариант C)

**Цвет = тип события:**
- MESSAGE / MESSAGE_RECEIVED — серый (#9E9E9E)
- DIALOGUE_FRAGMENT_PUBLISHED — синий (#2196F3)
- AGENT_ACTIVITY — зеленый (#4CAF50)
- NOTIFICATION — оранжевый (#FF9800)
- SYSTEM — фиолетовый (#9C27B0)

**Иконка/тикер = агент (для agent events):**
- TaskManager — 📋
- ContextManager — 🧠
- ContextAgent — 🎯
- Другие — 🤖

**Для user events:**
- User — 👤

---

### 4.5 Users Panel Data

**Структура пользователя:**
```typescript
interface UserPanelData {
  user_id: string
  name?: string

  // Последние действия
  last_sent?: Array<{
    content: string
    timestamp: ISO8601
  }>

  last_received?: Array<{
    content: string
    timestamp: ISO8601
    from: string
  }>

  // Состояние
  is_active: boolean
  current_dialogue_state?: 'active' | 'idle'
}
```

---

## 5. API

### 5.1 Эндпоинты

#### GET /api/timeline

Получить события для timeline с пагинацией по времени.

**Query params:**
- `since` (ISO8601, optional) — временная метка начала
- `limit` (number, optional, default 100) — лимит событий
- `swimlane_filter` (string[], optional) — фильтр по swimlanes

**Response:**
```json
{
  "events": [TimelineEvent],
  "next_since": ISO8601  // Для следующего запроса
}
```

---

#### GET /api/users

Получить состояние пользователей для Users Panel.

**Query params:**
- `since` (ISO8601, optional) — обновления с момента

**Response:**
```json
{
  "users": [UserPanelData]
}
```

---

#### GET /api/event/:id

Получить детализацию конкретного события.

**Response:**
```json
{
  "event": TimelineEvent,
  "related_events": [TimelineEvent]  // Опционально: связанные события
}
```

---

#### POST /api/sim/control

Управление симуляцией.

**Request body:**
```json
{
  "action": "start" | "stop" | "resume"
}
```

**Response:**
```json
{
  "status": "running" | "paused" | "stopped",
  "message": string
}
```

---

#### GET /api/sim/status

Получить статус симуляции.

**Response:**
```json
{
  "status": "running" | "paused" | "stopped",
  "started_at": ISO8601,
  "events_count": number
}
```

---

### 5.2 Polling стратегия

**Auto-mode (SIM running):**
- Polling каждые 1 секунду (настраиваемый до 3 сек)
- Загружаем события с `since` последнего запроса
- Auto-scroll к новым событиям

**History mode (SIM stopped):**
- Загружаем последние X минут (10 по умолчанию)
- Нет auto-scroll
- Настраиваемый лимит времени

**Memory management:**
- Храним события за последние X минут в браузере
- Лимит настраивается (10 минут по умолчанию)
- Старые события удаляются из памяти

---

## 6. Технологический стек

### 6.1 Frontend

| Компонент | Технология | Обоснование |
|-----------|------------|-------------|
| **Framework** | React + TypeScript | Типизация, экосистема |
| **Visualization** | D3.js | Свобода для кастомного вертикального timeline |
| **Styling** | TailwindCSS | Быстрая верстка |
| **Build** | Vite | Быстрый dev server |
| **State Management** | React Query | Синхронизация с API, caching |

### 6.2 Backend

| Компонент | Технология | Обоснование |
|-----------|------------|-------------|
| **Framework** | FastAPI | Async, auto OpenAPI |
| **Event Tracker** | Кастомный модуль | Фиксирует события для timeline |

---

## 7. Потоки данных

### 7.1 От SIM в Users Panel

```
SIM Engine → DialogueAgent.invoke()
    → Message stored in Storage
    → Users Panel polling (GET /api/users)
    → Display sent/received
```

### 7.2 От DialogueAgent в Timeline

```
DialogueAgent → DialogueFragmentPublished → EventBus
    → Event Tracker фиксирует событие
    → Stored in Storage (events table)
    → Timeline Panel polling (GET /api/timeline)
    → Display event with links
```

### 7.3 От ProcessingAgent в Timeline

```
ProcessingAgent → EventBus (agent_activity_start)
    → Event Tracker фиксирует
    → ProcessingAgent обрабатывает
    → EventBus (agent_result) → Event Tracker
    → Timeline Panel polling → display
```

### 7.4 Links между событиями

```
Event A (dialogue_fragment)
    → ProcessingAgent видит event A
    → Event B (agent_activity_start) with links.triggered_by = [A.id]
    → ProcessingAgent завершает
    → Event C (agent_result) with links.triggered_by = [B.id]
```

В Timeline Panel:
- Клик на Event C → Details Panel показывает "Triggered by: Event B"
- Клик на ссылку → прыжок к Event B

---

## 8. Ограничения MVP

### 8.1 НЕ входит в MVP

- Graph View (связи между сущностями)
- Demo Flow View (анимированная демонстрация)
- Replay режима (проиграть историю заново)
- Export/Save timeline
- Поиск по событиям
- Сложные фильтры
- Nested display на timeline

### 8.2 Технические ограничения

- **Масштаб:** < 50 пользователей
- **Events rate:** Ожидается умеренный поток событий
- **Browser memory:** Ограничено X минут событий (10 по умолчанию)

---

## 9. На будущее

### 9.1 Возможные улучшения

**Визуализация связей:**
- Кривые Безье между событиями на timeline
- Color coding по агентам (не только по типам)
- Interactive graph view

**Swimlanes:**
- Параллельные timeline с разными фильтрами
- Визуальная группировка на самом timeline

**Demo:**
- Анимированный Demo Flow View для лендинга
- Replay режима для воспроизведения истории

**UX:**
- Поиск по событиям
- Экспорт timeline в JSON/PNG
- Сложные фильтры (по времени, по типу, по агенту)

### 9.2 Архитектурные решения для будущего

**ADR TBD:**
- Выбор библиотеки для graph visualization
- Стратегия кэширования событий в браузере
- Real-time updates (WebSocket vs Polling)
- Экспорт и сохранение timeline

---

**Документ:** `00_docs/architecture/visualization_architecture.md`
**Последнее обновление:** 2025-01-24
